# win-activator
A PowerShell script to automate Windows activation using MAS.

This script provides a silent, programmatic way to activate Windows operating systems.

It utilizes the Microsoft Activation Script (MAS) HWID method for consumer editions
(Windows 10/11) and the TSForge method for Windows Server 2019 and newer versions.
